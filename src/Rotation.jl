rotx(θ) = [
    1 	0 		0 
    0 	cos(θ) -sin(θ)
    0 	sin(θ) 	cos(θ)
]

roty(θ) = [
    cos(θ) 	0 	sin(θ)
    0 		1 	0
    -sin(θ) 	0 	cos(θ)
]

rotz(θ) = [
    cos(θ) -sin(θ) 	0
    sin(θ) 	cos(θ) 	0
    0 		0 		1
]